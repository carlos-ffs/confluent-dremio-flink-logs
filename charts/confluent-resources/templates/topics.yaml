apiVersion: platform.confluent.io/v1beta1
kind: KafkaTopic
metadata:
  name: fluent-bit
  namespace: {{ .Release.Namespace }}
spec:
  replicas: 1
  partitionCount: 1
  configs:
    cleanup.policy: "delete"