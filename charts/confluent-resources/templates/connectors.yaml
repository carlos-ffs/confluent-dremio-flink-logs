apiVersion: platform.confluent.io/v1beta1
kind: Connector
metadata:
  name: fluent-bit-dremio-sink
spec:
  class: "org.apache.iceberg.connect.IcebergSinkConnector"
  name: "iceberg-sink-connector"
  taskMax: 2
  connectRest:
    endpoint: https://connect.confluent.svc.cluster.local:8083
    authentication:
      type: bearer
      bearer:
        secretRef: mds-client
  connectClusterRef:
    name: connect
    namespace: {{ .Release.Namespace }}
  configs:
    topics: "fluent-bit"
    iceberg.tables: "fluent_bit.logs"
    iceberg.tables.auto-create-enabled: "true"
    iceberg.catalog.type: "rest"
    iceberg.catalog.uri: "http://dremio-catalog-server.dremio.svc.cluster.local:9181/api/catalog"
    iceberg.catalog.warehouse: "default"
    iceberg.catalog.rest.oauth2-server-uri: "http://dremio.dremio.svc.cluster.local:9047/oauth/token"
    iceberg.catalog.credential: "Bq/hha37R++jq23yAZKQ8c+1b1ipHo06U1be3xduTq7X0Dx98dLEAPChBOpKZA=="
    #iceberg.catalog.rest.token: "Bq/hha37R++jq23yAZKQ8c+1b1ipHo06U1be3xduTq7X0Dx98dLEAPChBOpKZA=="
    iceberg.catalog.rest.header.X-Iceberg-Access-Delegation: "vended-credentials"
    # Connector behavior settings
    iceberg.control.commit.interval-ms: "60000"
    iceberg.control.commit.timeout-ms: "10000"
    # Data format settings
    value.converter: "org.apache.kafka.connect.json.JsonConverter"
    value.converter.schemas.enable: "false"
    key.converter: "org.apache.kafka.connect.storage.StringConverter"
